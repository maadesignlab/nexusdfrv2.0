export const routes = {
  dashboard: (locale) =>
    `/${locale}/dashboard`,

  library: (locale) =>
    `/${locale}/library`,

  coworking: (locale) =>
    `/${locale}/coworking`,

  account: (locale) =>
    `/${locale}/account`,

  cart: (locale) =>
    `/${locale}/cart`,

  checkout: (locale) =>
    `/${locale}/checkout`,
};